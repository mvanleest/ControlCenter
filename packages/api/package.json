{
  "name": "@control-center/api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm exec vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1031.0",
    "@aws-sdk/client-s3": "^3.1031.0",
    "@aws-sdk/client-sqs": "^3.1031.0",
    "@aws-sdk/lib-dynamodb": "^3.1037.0",
    "@control-center/domain": "workspace:*",
    "@control-center/provider-adapters": "workspace:*",
    "@hono/zod-validator": "^0.7.6",
    "hono": "^4.12.14",
    "sst": "^4.7.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  }
}
