# ControlCenter

Minimal `pnpm` monorepo scaffold.

## Structure

- `apps/*` for deployable applications
- `packages/*` for shared packages

## Getting started

1. Add a package inside `apps/` or `packages/`.
2. Run `pnpm install` at the repository root.
3. Use the root scripts to run tasks across workspaces.
